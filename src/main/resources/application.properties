spring.application.name=run-id-generation-service
server.port=8080

# Which environment-specific file to load (application-dev.properties or
# application-prod.properties below). Override at runtime without rebuilding:
#   java -jar run-id-generation-service.jar --spring.profiles.active=prod
# or by setting an environment variable before running:
#   SPRING_PROFILES_ACTIVE=prod java -jar run-id-generation-service.jar
spring.profiles.active=dev
